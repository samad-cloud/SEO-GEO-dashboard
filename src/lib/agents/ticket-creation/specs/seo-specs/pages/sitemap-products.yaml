page_id: sitemap-products
description: |
  Server-side XML route that returns the products sitemap.
  Reads pre-generated XML from dist/sitemap-q/{website}/sitemap-products.xml.

url:
  pattern: "/sitemap-products.xml"
  layout: null
  i18n_rewrites: {}

seo_contract:
  content_type: "application/xml"
  description: "Returns pre-generated XML sitemap of all product pages"
  no_html_head: true

file_chain:
  route:
    file: "src/routes/sitemap-products.xml/index.tsx"
    spec: "../migration-specs/frontend/routes/sitemap-products.yaml"
    export_type: "onGet (RequestHandler)"
  data_source:
    description: "Reads file from filesystem: dist/sitemap-q/{website}/sitemap-products.xml"
    dependencies: ["fs/promises", "path"]
    error_handling: "Returns empty XML on read failure"
    website_code: "Case-insensitive match from settings().website"
